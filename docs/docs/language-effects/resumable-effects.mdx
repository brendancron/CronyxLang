---
sidebar_position: 3
---

# Resumable effects

Coming soon. Introduces `ctl` operations and the `resume` keyword. Uses an `ask` effect as the running example — a handler computes a value and resumes the calling code with it. Sets up the comparison between `fn` (no resumption) and `ctl` (with resumption) operations.
