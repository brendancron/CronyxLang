---
sidebar_position: 7
---

# Multiple resumption

Coming soon. The chapter that distinguishes effects from exceptions and coroutines: a single `ctl` operation can call `resume` *more than once*, exploring multiple branches of computation from one call site. Worked example: a `flip` effect for nondeterministic choice, used to enumerate all possible outcomes. Brief mention of logic programming as a downstream application.
