---
sidebar_position: 2
---

# A first effect

Coming soon. Walks through the full declare/perform/handle lifecycle on the simplest case: a `log` effect declared with `fn` (fire-and-forget). Introduces the three-part pattern that every effect follows.
