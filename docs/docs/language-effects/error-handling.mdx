---
sidebar_position: 4
---

# Error handling

Coming soon. Covers error handling as an effect: a `ctl` operation that *omits* `resume` to abort the surrounding computation. Direct comparison to `try`/`catch` from other languages — what's familiar, what's different, and why the effect-based approach generalizes (handler can recover with a value, retry, or transform the error). Side-by-side with a Java/JS-style snippet so the parallel is concrete.
