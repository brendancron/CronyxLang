---
sidebar_position: 5
---

# Generators

Coming soon. Builds a generator-style stream using a `yield` effect — `ctl` operations called inside a loop, with the handler resuming after each value to drive the producer forward. Comparison to Python/JS generators and Rust iterators, showing that "yield" is just a particular use of resumable effects.
