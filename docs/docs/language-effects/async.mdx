---
sidebar_position: 8
---

# Case study: async

Coming soon. Capstone chapter showing cooperative multitasking implemented entirely as an effect handler — `spawn` and `tick` operations with a handler that maintains a task queue. Demonstrates that async/await isn't a language feature in Cronyx; it's a library pattern built on the same machinery as logging and error handling.
