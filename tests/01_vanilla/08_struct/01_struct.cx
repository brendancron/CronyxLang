// Nominal declaration — defines the type and its identity
type Person = {
    name: string,
    age: int
}

// Structural literal — satisfies Person without mentioning it
var p = {
    name: "Alice",
    age: 30
};

print(p);