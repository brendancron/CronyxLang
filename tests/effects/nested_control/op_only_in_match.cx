effect Ask { ctl get(): int; }

type Color { Red, Blue }

fn use(c: Color): <Ask> int {
    match c {
        Color::Red => { return get(); }
        Color::Blue => { return 0; }
    }
}

run { print(use(Color::Red)); }
handle Ask { ctl get() { resume 5; print("after"); } }
