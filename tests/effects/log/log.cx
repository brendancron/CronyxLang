// Phase 2: Basic fn handler — installs a log function and calls it.

effect Log {
    fn log(msg: string): unit;
}

with fn log(msg: string): unit {
    print("Log: " + msg);
}

log("Hello, World!");
