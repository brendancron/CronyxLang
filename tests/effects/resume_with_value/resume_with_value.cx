// Phase 3: resume passes a value back to the operation call site.
// The ctl operation returns the value that resume provides.

effect ask {
    ctl ask(): int;
}

with ctl ask(): int {
    resume 42;
}

var answer = ask();
print(answer);
