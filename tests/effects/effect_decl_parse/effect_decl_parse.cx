// Phase 1: Effect declaration should parse without error.
// No runtime behavior expected — just verify the pipeline doesn't crash.

effect log {
    fn log(msg: string): unit;
}

effect yield {
    ctl yield(i: int): unit;
}

effect flip {
    ctl flip(): bool;
}

print("parsed");
