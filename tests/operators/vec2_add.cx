struct Vec2 {
    x: int;
    y: int
}

impl Add for Vec2 {
    fn add(a: Vec2, b: Vec2): Vec2 {
        return Vec2 { x: a.x + b.x, y: a.y + b.y };
    }
}

var v1 = Vec2 { x: 1, y: 2 };
var v2 = Vec2 { x: 3, y: 4 };
var v3 = v1 + v2;
print(v3.x);
print(v3.y);
