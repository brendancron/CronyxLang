type Slot<...Args> {
    tag: string,
    intent: (...Args)
}

impl Slot<...Args> {
    fn label(self): string { return self.tag; }
}

var two = new Slot { tag: "two", intent: (1, "x") };
var none = new Slot { tag: "none", intent: () };

print(two.label());
print(none.label());
