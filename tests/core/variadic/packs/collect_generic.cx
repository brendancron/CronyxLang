type Slot<...Args> {
    use: (...Args) -> unit
}

fn hold<...Args>(s: Slot<...Args>, args: ...Args): (...Args) {
    return args;
}

var two = new Slot { use: (a: int, b: string) => { print(str(a) + b); } };
var held = hold(two, 1, "x");

print(held.0);
print(held.1);
print(typeof(held).name);
