fn apply_all<...Args>(f: (...Args) -> unit, args: ...Args) {
    f(...args);
}

fn pair(a: int, b: string) {
    print(str(a) + b);
}

fn flag(a: bool) {
    print(a);
}

fn nothing() {
    print("none");
}

apply_all(pair, 1, "x");
apply_all(flag, true);
apply_all(nothing);
