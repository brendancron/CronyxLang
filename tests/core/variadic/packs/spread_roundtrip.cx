fn apply_all<...Args>(f: (...Args) -> unit, args: ...Args) {
    f(...args);
}

apply_all((a: int, b: string) => { print(str(a) + b); }, 1, "x");
apply_all((a: bool) => { print(a); }, true);
apply_all(() => { print("none"); });
