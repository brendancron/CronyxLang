type Slot<...Args> {
    use: (...Args) -> unit
}

fn fire<...Args>(s: Slot<...Args>, args: (...Args)) {
    s.use(...args);
}

var two = new Slot { use: (a: int, b: string) => { print(str(a) + b); } };
var none = new Slot { use: () => { print("none"); } };

fire(two, (1, "x"));
fire(none, ());
