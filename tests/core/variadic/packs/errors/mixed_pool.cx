type Slot<...Args> {
    use: (...Args) -> unit
}

var two = new Slot { use: (a: int, b: string) => { print(str(a) + b); } };
var one = new Slot { use: (a: int) => { print(a); } };

var both = [two, one];
