type Slot<...Args> {
    use: (...Args) -> unit
}

fn pair(a: int, b: string) {
    print(str(a) + b);
}

fn flag(a: bool) {
    print(a);
}

var two = new Slot { use: pair };
var one = new Slot { use: flag };

two.use(1, "x");
one.use(true);
print(typeof(two).name);
print(typeof(one).name);
