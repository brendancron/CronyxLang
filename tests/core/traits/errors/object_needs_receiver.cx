trait Maker {
    fn make(): int;
    fn show(self): string;
}

type Cat;

impl Maker for Cat {
    fn make(): int {
        return 1;
    }

    fn show(self): string {
        return "cat";
    }
}

var maker: Maker = Cat;
