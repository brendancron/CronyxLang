effect Ask { ctl get(): int; }

trait Speaker {
    fn speak(self): string;
}

type Cat;

impl Speaker for Cat {
    fn speak(self): string {
        return "meow " + str(get());
    }
}

var s: Speaker = Cat;
