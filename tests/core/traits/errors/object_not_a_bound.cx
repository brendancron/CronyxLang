trait Speaker {
    fn speak(self): string;
}

type Cat;

impl Speaker for Cat {
    fn speak(self): string {
        return "meow";
    }
}

fn announce<S: Speaker>(s: S) {
    print(s.speak());
}

var sp: Speaker = Cat;
announce(sp);
