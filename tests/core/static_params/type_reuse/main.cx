fn wrap<T>(x: T): Array<T> {
    return [x];
}

fn swap<A, B>(pair: (A, B)): (B, A) {
    return (pair.1, pair.0);
}

var ints    = wrap(7);
var strings = wrap("hi");
var bools   = wrap(false);

print(ints[0]);
print(strings[0]);
print(bools[0]);

var swapped_nums = swap((1, 2));
var swapped_mix  = swap(("left", 42));

print(swapped_nums.0);
print(swapped_nums.1);
print(swapped_mix.0);
print(swapped_mix.1);
