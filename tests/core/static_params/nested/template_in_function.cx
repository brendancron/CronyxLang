fn main(): unit {
    fn repeat<n: int>(s: string): string {
        var out = "";
        var i = 0;
        while (i < n) { out = out + s; i = i + 1; }
        return out;
    }
    print(repeat<3>("ab"));
}
main();
