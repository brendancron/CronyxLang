struct Counter {
    value: int
}

var c = Counter { value: 0 };
c.value = c.value + 1;
c.value = c.value + 1;
c.value = c.value + 1;
print(c.value);

struct Point {
    x: int;
    y: int
}

var p = Point { x: 10, y: 20 };
p.x = p.x + 5;
p.y = 0;
print(p.x);
print(p.y);
