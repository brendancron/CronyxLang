fn ping(n: int) {
    meta print("ping");
    if (n == 0) return;
    print("ping " + str(n));
    pong(n - 1);
}

fn pong(n: int) {
    meta print("pong");
    if (n == 0) return;
    print("pong " + str(n));
    ping(n - 1);
}

ping(3);
