meta {
    var names = ["alice", "bob", "charlie"];
    for (name in names) {
        var fn_name = ("greet_" + name).as_name();
        gen fn fn_name() {
            print("Hello " + name);
        }
    }
}

greet_alice();
greet_bob();
greet_charlie();