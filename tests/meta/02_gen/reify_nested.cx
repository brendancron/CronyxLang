type P { x: int }
type E { A, B(P) }
meta {
    var es = [E::A, E::B(new P { x: 7 })];
    gen var all = es;
}
for (e in all) {
    match e {
        E::A => { print("A"); }
        E::B(p) => { print(typeof(p).name); print(p.x); }
    }
}
