(library
 (public_name bootstrap)
 (name bootstrap)
 ; `Toolchain` resolves the running binary's real path, so that a `cx` reached
 ; through a symlink still finds the standard library beside the real one.
 (libraries unix))
